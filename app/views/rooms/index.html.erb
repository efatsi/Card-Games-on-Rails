<h1>Listing rooms</h1>

<table>
  <tr>
    <th></th>
  </tr>

<% @rooms.each do |room| %>
  <tr>
    <td><%= link_to "Room #{room.id}", room %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Room', new_room_path %>
