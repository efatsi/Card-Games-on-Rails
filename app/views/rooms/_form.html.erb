<%= simple_form_for(@room) do |f| %>

  <div class="field">
    <%= f.input :name %>
    <%= f.input :game_type, :collection => Room::GAMES, :prompt => "Select a Game" %>
  </div>

  <div class="actions">
    <%= submit_tag "Create Room", :class => "btn"  %>
  </div>
<% end %>